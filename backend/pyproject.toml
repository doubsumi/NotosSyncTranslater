[project]
name = "notos-sync-translate-backend"
version = "0.1.0"
description = "Real-time two-way sync translator backend (Flask + free translation engines)"
readme = "../README.md"
requires-python = ">=3.9"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]
